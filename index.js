module.exports = function(sails) {

    //var setup = require('./lib/install.js');
    //var start = require('./lib/start.js');

    return {
        //install: install,
        //start: start,
    };
};
